export {
  mapper, mapper as columnsMapper,
  iterate, iterate as columnsIterate,
} from './src/mapper.js'

export {
  indexed, indexed as columns,
  indexedBy, indexedBy as columnsBy,
  indexedOf, indexedOf as columnsOf,
  indexedTo, indexedTo as columnsTo,
} from './src/indexed.js'
